spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mathdb?allowPublicKeyRetrieval=true&useSSL=false
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: cdelossantos
    password: cdelossantos
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
