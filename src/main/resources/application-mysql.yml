spring:
  datasource:
    url: jdbc:mysql://math-db:3306/mathdb?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: cdelossantos
    password: cdelossantos
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
